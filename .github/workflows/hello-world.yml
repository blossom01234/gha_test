name: Hello World

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened]  # open時のみトリガー（任意）


jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello, World!"
